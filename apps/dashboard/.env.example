VITE_API_URL=/api
VITE_WS_URL=/ws
# The dashboard authenticates with email/password (JWT); no API key needed here.
# For local dev the Vite proxy target is set via MC_PROXY_TARGET (see vite.config.ts).
MC_PROXY_TARGET=http://localhost:8099
